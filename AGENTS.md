# ShriKundali / Rashi Kundali — Agent Instructions

## Start of every session
Read `TODO.md` at the repo root before doing anything. It is the single source of truth
for open work — update it in place as tasks progress (keep the status legend).

## Repo
Monorepo: `packages/web/kundaliweb` (Astro frontend), `packages/backend/kundaliapi`
(Hono API), `packages/tools/` (videogen, assetgen). `README.md` has layout + commands.

## Key docs
- `docs/prd/` — frontend PRD, gap plan, Google Ads setup, AI image prompts
- `docs/ads/` — campaign plans, copy, creative assets, Meta/Google setup
- `docs/progress/` — launch checklist and status reports

## Rules
- Never commit secrets/credentials; reference where creds are needed instead.
- Paid traffic is blocked on P0 frontend items in `docs/prd/frontend-gap-plan.md`.
