# ShriKundali / Rashi Kundali — Project Context

Premium Kundli PDF report storefront + backend (monorepo). See `README.md` for layout
and `CHANGELOG.md` for history.

## Working agreement

- **Always read `@TODO.md` at the start of a session** — it is the single source of
  truth for open work. Update it in place as tasks progress.
- Key docs: `docs/prd/` (product/design/ads specs), `docs/ads/` (campaign + creative
  assets), `docs/progress/` (status + checklists).
- Never commit secrets or credentials. Ad/API creds are referenced, never stored, in git.
- Do not launch paid traffic until the P0 frontend items in
  `docs/prd/frontend-gap-plan.md` are done.

## TODO (imported from TODO.md)

@TODO.md
